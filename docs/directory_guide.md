### Directory Guide for Developer.

`controllers/` – Handles all HTTP requests
`services/` – Contains core business logic
`models/` – Defines all database schemas
`events/` – Event-driven components and handlers
`sockets/` – Real-time communication layer (WebSockets)
`config/` – Database connections, environment, and socket setup
